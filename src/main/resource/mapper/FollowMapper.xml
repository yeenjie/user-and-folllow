<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yej.ssm.mapper.CategoryMapper">
    <select id="getFollowId" parameterType="User" resultType="Follow">
          select * from   follow,user_ where follow.id=${id} and id!=${id}
    </select>

    <insert id="addFollowId" >
          Insert into
    </insert>

</mapper>